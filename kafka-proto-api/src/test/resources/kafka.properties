#
# the byte buffer pool size (shared across receives and sends)
spring.embedded.kafka.brokers=localhost:9092_test
#the test groupId
consumer.group.id=test_consumer
consumer.consumer.timeout.ms=50
sender.group.id=test_sender
common.prop.test=common_prop_test_value